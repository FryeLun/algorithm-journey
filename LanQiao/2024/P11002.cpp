#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    int t;
    scanf("%d", &t);
    while(t--) {
        int y, m, d, h, min, s;
        scanf("%d-%d-%d %d:%d:%d", &y, &m, &d, &h, &min, &s);
        
    }
    return 0;
}
